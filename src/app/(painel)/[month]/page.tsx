import { TabPlaceholder } from "@/features/shell/Shell";

export default function VisaoGeralPage() {
  return (
    <TabPlaceholder
      title="Visão geral"
      note="KPIs, fluxo de 6 meses, gastos por grupo e vencimentos — passo 19 do plano."
    />
  );
}
