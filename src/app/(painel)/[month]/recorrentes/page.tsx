import { TabPlaceholder } from "@/features/shell/Shell";

export default function RecorrentesPage() {
  return (
    <TabPlaceholder
      title="Assinaturas e parcelas"
      note="Linha do tempo de faturamento e cobranças recorrentes — passo 16."
    />
  );
}
