import { TabPlaceholder } from "@/features/shell/Shell";

export default function LancamentosPage() {
  return (
    <TabPlaceholder
      title="Lançamentos"
      note="Tabela com busca, filtro por tipo e por meio de pagamento — passo 15."
    />
  );
}
