import { TabPlaceholder } from "@/features/shell/Shell";

export default function ContasPage() {
  return (
    <TabPlaceholder
      title="Contas a pagar"
      note="Calendário 6×7, lista com baixa e filtro fixas/variáveis — passo 16."
    />
  );
}
